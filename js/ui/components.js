export function badge(label, cls='') { return `<span class="pill ${cls}">${label}</span>`; }
export function card(title, body='') { return `<section class="card"><h2>${title}</h2>${body}</section>`; }
