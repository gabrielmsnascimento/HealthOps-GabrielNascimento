import {Store} from './core/storage.js';import {renderDashboard} from './ui/dashboard.js';import {renderSchedule,bindSchedule} from './ui/schedule.js';import {renderMedications,bindMedications} from './ui/medications.js';import {renderDebug} from './ui/debug.js';
const state=Store.load();const routes={inicio:['Início',()=>renderDashboard(state)],escala:['Escala',()=>renderSchedule(state),()=>bindSchedule(state)],medicacoes:['Medicações',()=>renderMedications(state),()=>bindMedications(state)],debug:['Debug',()=>renderDebug(state)]};
async function render(route='inicio'){try{document.getElementById('nav').innerHTML=Object.entries(routes).map(([k,v])=>`<button class="${k===route?'active':''}" data-route="${k}">${v[0]}</button>`).join('');document.querySelectorAll('[data-route]').forEach(b=>b.onclick=()=>render(b.dataset.route));document.getElementById('app').innerHTML=await routes[route][1]();routes[route][2]?.();}catch(e){document.getElementById('app').innerHTML=`<div class="card"><h2>Erro nesta aba</h2><pre class="error">${e.stack||e.message}</pre></div>`;Store.log(state,e.stack||e.message)}}
function modal(t,b){document.getElementById('modalTitle').textContent=t;document.getElementById('modalBody').innerHTML=typeof b==='string'?`<p>${b}</p>`:b;document.getElementById('modal').classList.add('on')}function closeModal(){document.getElementById('modal').classList.remove('on')}window.HealthOps={render,modal,closeModal};render();if('serviceWorker'in navigator)navigator.serviceWorker.register('./service-worker.js').catch(()=>{});
