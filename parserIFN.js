import {recoveryForJourneys} from '../core/operational-engine.js';
export function renderDashboard(state){const data=state.imports[state.activeMonth];const s=data?.summary||{days:0,duty:0,flight:0,rests:0};return `<div class="grid cards"><div class="card"><b>Dias</b><div class="metric">${s.days}</div></div><div class="card"><b>Jornada</b><div class="metric">${s.duty}h</div></div><div class="card"><b>Voo</b><div class="metric">${s.flight}h</div></div><div class="card"><b>Descanso</b><div class="metric">${s.rests}</div></div></div><br><div class="card"><h2>Prontidão operacional</h2><p class="sub">Selecione/importe uma escala para estimar repouso oficial, recuperação de hotel e recomendações de treino.</p>${data?quick(recoveryForJourneys(data.journeys)):''}</div>`}
function quick(js){const bad=js.filter(j=>j.recovery?.status==='bad').length;return `<span class="pill ${bad?'bad':'ok'}">${bad?bad+' alertas de hotel':'Sem alerta crítico de hotel'}</span>`}
